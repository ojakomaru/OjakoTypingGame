import React from 'react'
import { FormControl, styled } from '@mui/material';

const StyledRadioForm = styled(FormControl)((props) => ({
  fontWeight: "bold",
  fontSize: 18,
}));

export default StyledRadioForm